<view class="card {{desc ? 'card-desc' : 'card-title'}}" style="background-color: {{bgColor}}" hover-class="card-hover">
  <text class="title">{{title}}</text>
  <text wx:if="{{desc}}" class="desc">{{desc}}</text>
</view>